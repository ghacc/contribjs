/* interface ContribConfig {
    proxyPort: number,
    targetUrl: string,
    rules: [
        {pattern, exclude/include, vary, headers}
    ]
} */

// Will define interface for configuration, and also read config file, organize it and expose it to any interested module